[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "daq-alert"
version = "1.0.0"
description = "Mu2e DAQ alert sender library"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["daq_alert*"]
